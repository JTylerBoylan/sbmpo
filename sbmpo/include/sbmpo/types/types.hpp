#ifndef SBMPO_TYPES_HPP
#define SBMPO_TYPES_HPP

#include <vector>

typedef std::vector<float> State;
typedef std::vector<float> Control;

typedef std::vector<Control> Branchouts;

typedef std::vector<int> GridKey;

#endif