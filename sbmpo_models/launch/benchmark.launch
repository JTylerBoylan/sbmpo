<launch>

    <arg name="model" value="grid_2d"/>

    <rosparam command="load" file="$(find sbmpo_models)/config/$(arg model)_model.yaml"/>
    <node pkg="sbmpo_models" type="benchmark" name="benchmark" output="screen">
        <param name="runs" value="1"/>
    </node>

</launch>
